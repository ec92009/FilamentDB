[project]
name = "filamentdb"
version = "0.1.1"
description = "Local SQLite filament database for TD1 readings and manual filament metadata."
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["PySide6>=6.8"]

[tool.uv]
package = false
